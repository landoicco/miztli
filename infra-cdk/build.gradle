plugins {
    id 'java'
    id 'application'

    id "com.diffplug.spotless" version "6.25.0"
}

group = 'licaza.miztli.iac.aws'
version = '1.0.0'

repositories {
    mavenCentral()
}

dependencies {
    implementation 'software.amazon.awscdk:aws-cdk-lib:2.110.0'
    implementation 'software.constructs:constructs:10.3.0'
    // Módulos Alpha para HTTP API
    implementation 'software.amazon.awscdk:apigatewayv2-alpha:2.114.0-alpha.0'
    implementation 'software.amazon.awscdk:apigatewayv2-integrations-alpha:2.114.0-alpha.0'
}

subprojects {
    apply plugin: "com.diffplug.spotless"
    spotless {
        java {
            googleJavaFormat()
            removeUnusedImports()
            trimTrailingWhitespace()
            endWithNewline()
        }
    }
}

application {
    mainClass = 'licaza.miztli.iac.aws.CdkApp'
}
